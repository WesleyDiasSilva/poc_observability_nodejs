version: "3.8"

services:
  node-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: node-app
    ports:
      - "4000:4000"
    volumes:
      - .:/app
    networks:
      - monitoring
    command: ["npm", "run", "dev"]

networks:
  monitoring:
